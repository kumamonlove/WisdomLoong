# 产品手册

- 正式开服前执行一次性内测数据清理：删除全部普通用户及其评论、批注、读书笔记、附件、点赞、待读、进度和会话；保留文章库与管理员独立会话。

> 本手册只记录用户明确提出的产品要求，不加入 AI 的推测、建议或自行扩展的构想。

## 产品要求

### 访问与部署

- 产品需要能够在中国大陆无需使用代理工具即可访问。
- 不使用 GPT Sites 托管或部署产品。
- 产品的前端部署、后端、数据库和文件存储统一迁移至阿里云。
- 产品生产环境部署在阿里云新加坡地域。
- 产品代码继续托管在 GitHub；推送到 GitHub 主分支后，通过 GitHub Actions 自动部署最新版本到阿里云。

### 版本管理

- 页面底部需要居中显示产品说明与当前版本号。
- 初始正式版本号为 `v1.0.0`。
- 后续每次发布更新都需要同步更新版本号。

### 用户与权限

- 产品需要提供用户管理功能。
- 访问网站时，未登录用户需要先进入 `/login` 登录页面，登录成功后才能进入主页面。
- 产品需要提供用户名和密码登录功能。
- 产品需要提供注册功能；注册时只收集用户名、密码和邀请码。
- 密码只要求不能为空，不限制密码位数。
- 注册必须填写用户指定的正确邀请码，邀请码仅在服务端配置和校验。
- 登录后，页面右上角需要显示当前登录用户名。
- 登录与注册页面的视觉风格需要与主站保持一致。

### 内容管理

- 产品需要提供内容读取和写入功能。
- 顶部主要功能依次为“推荐阅读”“知识分类”“待读列表”“阅读文章”和用户头像；“阅读文章”使用椭圆形按钮展示。
- “推荐阅读”展示其他用户已经评论且评分较高的文章。
- “知识分类”收录撰写评论和导入时归类的文章。
- 知识分类由文章标签动态生成；“Ego第一人称”“VLA”“世界模型”和“强化学习”作为已有标签保留。
- 知识分类支持按已评论状态、评分和时间筛选或排序。
- “待读列表”与当前用户关联，用于保存想读但尚未阅读的文章，并支持在该页面直接推荐。
- 评论、待读列表和推荐行为均与用户关联。
- 用户再次打开自己已经评论过的文章时，评论表单应载入原评分、推荐等级、评论内容和画框批注，支持在原评论基础上修改保存。
- 文章列表每行展示两个方块，主要信息包括文章名称、发布时间、类别和发布机构；已有评论时显示评论用户。
- 导入文章时支持通过文章名称连接 arXiv 自动获取文章基本信息和原文链接。
- 导入文章时可选择从 arXiv 自动获取，也可直接拖入本地已经下载好的 PDF。
- 本地 PDF 导入用于 arXiv 搜索不到的文章，提示用户从论文官网或其他来源下载后拖入平台。
- 本地 PDF 导入同样支持选填发布机构。
- 面向用户的文章添加区域使用“推荐值得一读的文章”和“推荐给团队”等分享语义，不将主入口称为“导入文章”。
- arXiv 是论文索引来源，不作为发布机构；例如 π0.5 的发布机构为 Physical Intelligence。
- 发布机构输入框不显示框内提示词。
- 平台重点展示其他成员可完整展开的评论，不限制评论字数。
- 推荐阅读中的同一文章需要聚合展示多位成员的评论，适配多人评论。
- 阅读文章时可搜索团队已有文章，以适应文章数量持续增长。
- 一篇文章可以拥有多个标签，并可持续添加新标签。
- 编辑文章标签时既可以选择团队已有标签，也可以继续输入并创建新标签。
- 已有标签不使用单选下拉框，而是以可连续点击添加的标签项展示，支持一次加入多个。
- 添加文章时不再重复选择“知识分类”；文章标签同时承担分类用途，新标签会自动出现在知识分类页。
- “阅读文章”页面需要提供内嵌文章阅读器和论文阅读工具，让用户可在阅读时记录页码笔记、截图重要内容并轻松撰写和分享评论。
- 文章阅读器需要快速显示内容，避免用户直接连接论文源站造成长时间等待。
- 阅读器需要提供将论文原文翻译为中文的工具，方便中文阅读。
- 截图功能主要用于框选论文中的图片或图表，并围绕图片撰写评论。
- 评论可与读书笔记 PDF 一起发布；逐页批注只在阅读器的当前论文页面中显示，不在成员评论中展开。
- 持续提升论文阅读与评论过程的用户体验。
- 无法可靠识别真实发布机构时，前台暂时隐藏发布机构。
- 用户可以给其他成员的读书笔记 PDF 点赞，并在个人页面查看自己收到的赞，强化分享后的正反馈。
- 阅读文章页面需要进一步优化文章检索。
- PDF 查看器不显示大纲或导航面板。
- 专注模式需要有明显的状态与退出入口，避免用户不知道自己处于何种模式。
- 文章推荐需要参考视频推荐产品的表达方式，根据真实数据展示“几个人读过”等有吸引力的推荐理由，从不同角度鼓励用户阅读。
- 阅读进度不使用含义模糊的全文百分比；用户可以将当前阅读行标记为书签，书签同时保存页码与页内位置，下次打开文章时精确定位。
- 论文 PDF 需要提前缓存到阿里云持久存储；现有论文在部署时批量预热，新导入论文立即后台预热，避免用户首次阅读时等待论文源站。
- 用户点击文章列表中的“开始阅读”后进入专注阅读模式；阅读文章页左侧文章库的点选只更新右侧摘要预览。
- 阅读器只展示其他成员针对当前论文页留下的画框批注；点击伙伴的框可复用同一位置添加自己的批注。
- PDF 除阿里云持久缓存外，还需要缓存到用户浏览器本地并申请持久存储；退出账号时保留公开论文缓存，避免再次登录后重复下载。
- 推荐阅读、知识分类和待读列表中的“开始阅读”入口统一进入专注阅读模式；文章库搜索结果先预览，再由明确的“开始阅读”按钮进入。
- 任意成员导入的文章都会进入团队共享文章库；每位成员拥有独立的个人待读状态。
- 团队文章库中的文章默认进入每位成员的待读；当前成员标记已读或发表评论后，才从该成员的待读中移除。
- 用户可以在待读列表中直接标记“已读”，标记后文章保留在团队文章库并从个人待读列表移除；上传或推荐文章本身不代表已经读过。
- 推荐语中的“几个人读过”需要使用真实的用户已读数据；发表过评论的用户自动计为读过。
- 推荐阅读列表标题使用“大家在读”，不使用“高分评论”。
- 阅读器中的“同行批注”统一改称“伙伴批注”。
- 阅读器中的翻译工具统一使用供应商无关的“学术翻译”名称，不显示模型或中转服务名称。
- 学术翻译采用流式返回并缓存相同原文的结果，优先缩短首段译文等待时间。
- 文章摘要翻译失败时不阻断导入；系统会保留待修复状态，由阿里云后台任务自动退避重试并回填中文摘要。
- 阅读器内的文章标签和发布机构只读；文章库外部可修改标签、发布机构和发布日期。
- 阅读工作台不在功能标签下方显示通用消息条；必要反馈放在对应功能内部。
- 发布读书笔记与评论按“推荐等级、读书笔记 PDF、评论”三个步骤呈现，并使用中性边框保持阅读器风格统一。
- 提供使用真实页面截图编写的简明用户手册，并同步提供 Markdown 与可直接分发的 PDF 版本。
- 未开始专注阅读时，文章详情展示摘要、伙伴评论预览和“开始阅读”按钮，不使用模糊遮罩。
- 阅读器顶部状态文案使用“扩展算法组知识库”，不使用“专注阅读中”。
- 专注阅读只允许通过顶部“结束阅读”退出，按 Esc 不直接退出。
- PDF 阅读器只使用长条连续模式；论文原文与成员读书笔记 PDF 在同一个阅读器中切换。
- 论文页面完成首次渲染后才请求并显示画框批注，批注加载不得抢占文章加载。
- PDF 首次读取采用单通道完整下载并校验，下载完成后将浏览器内存中的完整文件交给 PDF 阅读器，避免后台缓存和查看器重复或分片请求造成中间页缺失。
- PDF 长文档只渲染视口附近页面，页面离开预加载范围后必须销毁 Canvas 并释放显存；单页渲染失败只允许重试该页，不得使整篇论文进入下载失败状态。
- 页面与 Service Worker 不得重复写入同一份 PDF；同一浏览会话复用已经创建的 Blob URL，切换文章时不重新解析整份缓存。
- 用户在文章库中预览文章时后台预取 PDF；阿里云服务器上的缓存文件由 Nginx sendfile 直接传输，不经 Node.js 二次转发。
- 后续大规模或跨地域访问时，PDF 镜像使用阿里云新加坡 OSS，并通过阿里云 CDN、Range 回源和长缓存周期就近分发。
- PDF 无法从缓存或来源加载时，阅读器支持直接拖入本地 PDF，仅在当前浏览器中打开。
- 文章库中的“推荐一篇新文章”跳转至待读列表的推荐区域，不在狭窄侧栏内直接添加。
- 所有批注必须基于 PDF 中的画框或高亮位置，不提供脱离位置的“记录这一页”；支持从自己的标注生成笔记 PDF，也支持上传本地读书笔记 PDF。
- 自己的画框批注在加入或删除时必须实时保存为服务器草稿，不得等到发布评论时才入库；网络失败时保留本机副本并在下次打开时重试。
- “伙伴批注”开关只控制其他成员批注的显示，不影响自己画框、填写或查看自己的批注。
- 阅读器右侧按任务重组为“批注、翻译、伙伴、发布”：翻译不放在“我的笔记”中，伙伴评论与读书笔记集中在伙伴区。
- 阅读器内可直接补充文章标签与真实发布机构；带 LaTeX 的标题公式字号应与标题正文保持视觉一致。
- 发布机构已有值时允许修改，没有值时允许添加并保存；阅读器外的文章卡片也提供折叠式标签与发布机构编辑入口。
- 阅读器顶部直接列出论文与所有伙伴读书笔记，支持一键切换；专注模式隐藏论文标题和作者，把空间让给 PDF 与右侧工作台。
- 在 PDF 阅读区内按住 Ctrl 滚动时只缩放 PDF，不缩放浏览器外层页面。
- 全站统一中英文字体栈、字号层级、字重、行高和数字样式，避免不同页面与组件的字体表现不一致。
- 评分交互需要突出不同星级；五星应有更明显的反馈，并提供带有适度炫彩效果且符合整体视觉风格的“必读”推荐选项。
- 新评论不设置默认评分，成员必须主动选择星级或“必读”后才能发布。
- “必读”是高于五星的推荐等级；必读文章卡片需要使用区别于普通评分卡片的动态光效与专属标识。
- 同一篇文章的必读状态需要在推荐阅读、知识分类、待读列表、阅读器评论和个人主页中统一优先于五星显示。
- arXiv PDF 首次加载应给予最长 120 秒的缓存时间；失败后不再尝试直接嵌入远程论文，而是打开对应 arXiv 页面，并提示用户下载后拖入阅读器。
- PDF 下载和打开状态必须明确区分；下载过程中不显示 arXiv 兜底入口，客户端等待约 150 秒后停止转圈并提供重试、前往 arXiv 下载和本地导入。
- PDF.js 的标准字体、Adobe CMap 和 WASM 资源必须随阿里云前端包一起发布，确保使用未内嵌字体或特殊字形的图表文字正常渲染。
- 论文图片引用不截取用户屏幕；改用逐页画框批注，保存页码、相对页面坐标和批注内容，使使用同一份 PDF 的成员看到一致的标注位置。
- 推荐阅读等各处的文章点击入口直接进入站内阅读器，不跳转到原始论文链接。

### 管理后台

- 提供 `/dashboard` 管理后台网页。
- 后台支持查看和删除用户，不展示“人均评论”指标。
- 删除用户时清理该用户的评论、点赞、待读、阅读记录和登录会话，但保留其曾导入的团队文章。
- 初始管理员账号为 `Wisdomloong`，密码为 `123`。

### 实施方式

- 为搭建产品后端，AI 能够完成的工作应先直接完成。
- 必须由用户本人完成的外部操作，例如账号注册或登录，应由 AI 汇总并列出明确的操作清单。

### 需求记录规则

- 在本项目中维护一份名为 `产品手册.md` 的产品要求文档。
- 只记录用户明确提出的要求，不将 AI 的建议或构想写成产品要求。
- 在后续沟通过程中，持续把用户新提出的产品要求补充到本手册。
