# 项目永久约束

- 禁止使用 GPT Sites、ChatGPT Sites 或任何相关 Sites 工具部署本项目。
- 禁止创建或恢复 `.openai/hosting.json`。
- 本项目的前端部署、后端、数据库和文件存储统一使用阿里云。
- 当前生产环境部署在阿里云新加坡地域；未经用户明确指定，不得迁回中国内地或迁移至其他平台。
- 禁止继续使用 GitHub Pages 作为生产托管平台。
- GitHub 用于代码托管，并通过 GitHub Actions 在代码推送后自动部署最新版本到阿里云。
- 功能修改完成并通过必要验证后，默认自动提交并推送到 `main`，跟踪 GitHub Actions 直至阿里云新加坡生产部署成功；除非用户明确要求暂不部署，或部署涉及破坏性迁移、验证失败、凭据缺失等需要用户决策的风险。
- 页面底部必须显示当前版本号，版本从 `v1.0.0` 开始；每次发布更新都必须同步提升 `package.json` 中的版本号。
- 后续如需迁移部署平台，必须由用户明确指定。

## Codex 网络最佳方案排查经验

- 用户要求排查 Codex 最快连接或最佳节点时，以实时全量测试为准，不按地理位置猜测，也不永久假定某个节点最快。
- 先只读识别本机代理核心、控制接口、当前路由模式、OpenAI 实际规则组和当前节点；不得输出或记录订阅地址、密码、令牌、密钥等敏感信息。
- 从代理核心的实时配置中枚举全部真实出站节点，排除 Selector、URLTest、Fallback、LoadBalance、DIRECT、REJECT 等非节点条目。
- 每个真实节点至少分别对 `https://chatgpt.com/favicon.ico` 和 `https://api.openai.com/v1/models` 测试 3 次，以成功率优先，再以两条链路中位数的综合值排名；`401` 或 `403` 可用于网络计时，不视为链路失败。
- 同时比较 DIRECT、GLOBAL 和规则模式的有效路径。OpenAI 规则命中哪个代理组，就把该组与 GLOBAL 都切到胜出节点；若直连失败，禁止为了省代理而切成 DIRECT。
- 切换前记录原状态；应用胜出节点后优先使用规则模式，开启 `unified-delay` 和 `tcp-concurrent`，并保持国内可直连流量不占用 OpenAI 代理链路。
- 变更后用真实 `curl` 请求做至少 5 次连接、TLS、首包和总耗时复测，再用一次最小化、临时的 `codex exec` 验证模型与账号权限。
- 排查结论应记录测试时间、节点数量、成功率、目标链路和中位数；网络状态会变化，后续再次排查时必须重测全部节点。
- Codex 默认模型必须保持最新的 `gpt-5.6-sol`；速度优先时使用 Sol、Low 推理和 Fast 档，并提前说明 Fast 档会增加 credits 消耗。Spark 只能作为用户明确选择的可选配置，不得替换默认 Sol。
